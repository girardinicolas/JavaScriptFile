// Funzione dichiarata per calcolare la media di due numeri
function calcolaMedia(num1, num2) {
    return (num1 + num2) / 2;
}

// Esempio di utilizzo
const a = 8;
const b = 12;

console.log("Media di", a, "e", b, "è:", calcolaMedia(a, b));
